<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.core.mapper.UserMapper">

    <!-- Result Map -->
    <resultMap id="userResultMap" type="com.core.model.User">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="email" column="email"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <!-- 모든 사용자 조회 -->
    <select id="findAll" resultMap="userResultMap">
        SELECT id, username, email, created_at
        FROM users
        ORDER BY id DESC
    </select>

    <!-- ID로 사용자 조회 -->
    <select id="findById" parameterType="long" resultMap="userResultMap">
        SELECT id, username, email, created_at
        FROM users
        WHERE id = #{id}
    </select>

    <!-- 사용자명으로 사용자 조회 -->
    <select id="findByUsername" parameterType="string" resultMap="userResultMap">
        SELECT id, username, email, created_at
        FROM users
        WHERE username = #{username}
    </select>

    <!-- 사용자 등록 -->
    <insert id="insert" parameterType="com.core.model.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users (username, email, created_at)
        VALUES (#{username}, #{email}, NOW())
    </insert>

    <!-- 사용자 정보 수정 -->
    <update id="update" parameterType="com.core.model.User">
        UPDATE users
        SET username = #{username},
            email = #{email}
        WHERE id = #{id}
    </update>

    <!-- 사용자 삭제 -->
    <delete id="deleteById" parameterType="long">
        DELETE FROM users
        WHERE id = #{id}
    </delete>

    <!-- 전체 사용자 수 조회 -->
    <select id="count" resultType="int">
        SELECT COUNT(*)
        FROM users
    </select>

</mapper>
